<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">  
  <meta>  
    <sampleQuery>select * from {table} where url_path='www.example.com' and header='true';</sampleQuery>    </meta>  
  <bindings>  
    <select itemPath="" produces="XML">  
      <inputs>  
        <key id='url_path' type='xs:string' paramType='variable' required="true" />  
        <key id='username' type='xs:boolean' paramType='variable' required="true"/> 
        <key id='password' type='xs:boolean' paramType='variable' required="true"/> 
      </inputs>  
      <execute><![CDATA[  
        y.include("http://yqlblog.net/samples/base64.js");  
        var authheader = "Basic "+Base64.encode(username+":"+password);  
        response.object = request.header("Authorization",authheader).get(url_path).response;  
        /*var res = {}; 
        res.body = y.rest(url_path).head().headers; 
        response.object=res.body; */
      ]]></execute>  
    </select>  
  </bindings>  
</table>  
